<%= form_for(@expense_tax_type) do |f| %>
  <% if @expense_tax_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@expense_tax_type.errors.count, "error") %> prohibited this expense_tax_type from being saved:</h2>

      <ul>
      <% @expense_tax_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
