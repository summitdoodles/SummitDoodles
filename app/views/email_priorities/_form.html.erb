<%= form_for(@email_priority) do |f| %>
  <% if @email_priority.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@email_priority.errors.count, "error") %> prohibited this email_priority from being saved:</h2>

      <ul>
      <% @email_priority.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
